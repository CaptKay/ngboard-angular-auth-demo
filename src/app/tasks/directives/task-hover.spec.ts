import { TaskHover } from './task-hover';

describe('TaskHover', () => {
  it('should create an instance', () => {
    const directive = new TaskHover();
    expect(directive).toBeTruthy();
  });
});
