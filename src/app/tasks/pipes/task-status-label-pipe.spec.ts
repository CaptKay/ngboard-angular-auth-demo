import { TaskStatusLabelPipe } from './task-status-label-pipe';

describe('TaskStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
